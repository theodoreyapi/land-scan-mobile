name: ticket_valide
description: "Application de validation de ticket."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sizer: ^3.0.5
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.3
  intl: ^0.19.0
  gap: ^3.0.1
  http: ^1.2.2
  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  path: ^1.9.1
  qr_code_scanner_plus: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true